<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= blog.title %></title> <!-- Dynamic title based on fruit name -->
  </head>
  <body>
    <h1><%= blog.title %></h1>
    <p><%= blog.body %></p>
    <!-- Conditional rendering based on fruit's readiness -->
  <% if (blog.isReadyToPost) { %>
    <p>This blog is ready to post!</p>
  <% } else { %>
    <p>This blog is not ready to post!</p>
  <% } %> <!-- Display fruit name -->
    <form action="/blogs/<%=blog._id%>?_method=DELETE" method="POST">
        <button type="submit">Delete <%= blog.title %></button>
    </form>
        <a href="/blogs/<%= blog._id %>/edit">Edit <%= blog.title %></a>
        <a href="/blogs/">Back to Blogs</a>
  </body>
</html>